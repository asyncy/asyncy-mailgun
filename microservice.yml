commands:
  send:
    help: Send an email
    arguments:
      to:
        type: string
        required: true
      from:
        type: string
        required: true
      bcc:
        type: string
      cc:
        type: string
      subject:
        type: string
        required: true
      text:
        type: string
      html:
        type: string
      attachment_path:
        type: string
      inline_attatchment_path:
        type: string      

environment:
  API_KEY:
    type: string
  DOMAIN:
    type: string
